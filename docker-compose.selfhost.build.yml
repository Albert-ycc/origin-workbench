# Development override: build the backend image from the current checkout
# instead of pulling the official GHCR image.
#
# Note: Origin fork removed the legacy Multica web frontend, so this file
# only declares the backend build.

services:
  backend:
    image: origin-backend:dev
    build:
      context: .
      dockerfile: Dockerfile
